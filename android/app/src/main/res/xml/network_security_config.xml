<?xml version="1.0" encoding="utf-8"?>
<!-- Разрешаем cleartext HTTP только для бэкенда (IP без TLS). ВРЕМЕННО для тестов.
     Для релиза перейти на https://домен и удалить это исключение. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">185.40.4.195</domain>
    </domain-config>
</network-security-config>
